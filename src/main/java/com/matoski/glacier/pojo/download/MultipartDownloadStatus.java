package com.matoski.glacier.pojo.download;

import com.matoski.glacier.pojo.ArchiveState;

/**
 * A state file, used to store the state of the downloading file, we use this to store all the
 * information necessary to continue downloading the file
 *
 * @author Ilija Matoski (ilijamt@gmail.com)
 */
public class MultipartDownloadStatus extends ArchiveState<MultipartDownloadStatus> {
}